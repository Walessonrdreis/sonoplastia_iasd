package com.example.viewmodel

import androidx.lifecycle.ViewModel
import com.example.data.repository.AppRepository

class AvailabilityViewModel(private val repository: AppRepository) : ViewModel() {
    // Placeholder
}
